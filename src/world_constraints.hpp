#pragma once

namespace WorldConstraints {
    const float SCALE_X = 16.0f;
    const float SCALE_Z = 18.0f;

    const float LEFT = -SCALE_Z + 1.0f;
    const float RIGHT = SCALE_Z - 1.0f;
    const float TOP = SCALE_X - 8.0f;
    const float BOTTOM = -SCALE_X - 4.0f;
}
